name: Deploy Grigou to Production

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '20'
  WORKING_DIRECTORY: './app'

jobs:
  # ============================================
  # JOB 1: Tests & Build Validation
  # ============================================
  test:
    name: Tests & Quality Checks
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ${{ env.WORKING_DIRECTORY }}
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'app/package-lock.json'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Run ESLint
        run: npm run lint || echo "⚠️ Lint warnings (non-blocking)"
        continue-on-error: true

      - name: 🏗️ Type check
        run: npx tsc --noEmit || echo "⚠️ Type errors (non-blocking)"
        continue-on-error: true

      - name: 🏗️ Build test
        run: npm run build
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/testdb
          NEXTAUTH_SECRET: test-secret-key-min-32-chars-long-for-build
          NEXTAUTH_URL: http://localhost:3000

      - name: ✅ Tests completed
        run: echo "✅ All checks completed!"

  # ============================================
  # JOB 2: Deploy to Production VPS
  # ============================================
  deploy:
    name: Deploy to VPS
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: 🚀 Deploy to Production
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} bash << 'ENDSSH'
            set -e
            
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🔵 Starting Grigou Deployment"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            # Variables
            APP_DIR="${{ secrets.APP_DIR }}"
            BACKUP_DIR="$APP_DIR/backups"
            
            cd "$APP_DIR"
            
            # Charger les variables d'environnement proprement
            echo "📋 Loading environment variables..."
            source .env.production
            
            echo "   Database: ${POSTGRES_DB}"
            echo "   User: ${POSTGRES_USER}"
            
            # Create backup
            echo "📦 Creating database backup..."
            mkdir -p "$BACKUP_DIR"
            BACKUP_FILE="$BACKUP_DIR/db-backup-$(date +%Y%m%d-%H%M%S).sql"
            
            # Backup avec les variables chargées
            if docker exec grigou_db pg_dump -U "${POSTGRES_USER}" "${POSTGRES_DB}" > "$BACKUP_FILE" 2>&1; then
              echo "✅ Backup created: $(basename "$BACKUP_FILE") ($(du -h "$BACKUP_FILE" | cut -f1))"
            else
              echo "⚠️ Backup failed (non-critical)"
            fi
            
            # Keep only last 5 backups
            ls -t "$BACKUP_DIR"/db-backup-*.sql 2>/dev/null | tail -n +6 | xargs -r rm || true
            
            # Ensure Git is using HTTPS
            echo "🔧 Configuring Git..."
            CURRENT_URL=$(git remote get-url origin)
            if [[ "$CURRENT_URL" == git@github.com:* ]]; then
              echo "   Converting SSH URL to HTTPS..."
              NEW_URL=$(echo "$CURRENT_URL" | sed 's|git@github.com:|https://github.com/|' | sed 's|\.git$||')
              git remote set-url origin "${NEW_URL}.git"
              echo "   New URL: $(git remote get-url origin)"
            fi
            
            # Pull latest changes
            echo "📥 Pulling latest changes from GitHub..."
            git fetch origin
            git reset --hard origin/main
            CURRENT_COMMIT=$(git rev-parse --short HEAD)
            echo "✅ Code updated to commit: $CURRENT_COMMIT"
            
            # Stop containers - UTILISER docker compose (V2)
            echo "🛑 Stopping containers..."
            docker compose -f docker-compose.prod.yml down || true
            
            # Build new images - UTILISER docker compose (V2)
            echo "🔨 Building new Docker images..."
            docker compose -f docker-compose.prod.yml build --no-cache
            
            # Start containers - UTILISER docker compose (V2)
            echo "🚀 Starting containers..."
            docker compose -f docker-compose.prod.yml up -d
            
            # Wait for containers to be healthy
            echo "⏳ Waiting for containers to start (30s)..."
            sleep 30
            
            # Check container status - UTILISER docker compose (V2)
            echo "📊 Container status:"
            docker compose -f docker-compose.prod.yml ps
            
            # Check if app is responding
            echo "🔍 Checking application health..."
            if docker exec grigou_app curl -f http://localhost:3000/api/health 2>/dev/null; then
              echo "✅ Application is responding"
            else
              echo "⚠️ Application health check failed (might still be starting)"
            fi
            
            # Cleanup old images
            echo "🧹 Cleaning up old Docker images..."
            docker image prune -af || true
            
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "✅ Deployment completed successfully!"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

      - name: 🧪 Health Check
        run: |
          echo "⏳ Waiting 10 seconds before health check..."
          sleep 10
          
          echo "🔍 Checking application health..."
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://${{ secrets.PRODUCTION_DOMAIN }}/api/health || echo "000")
          
          echo "HTTP Status: $HTTP_STATUS"
          
          if [ "$HTTP_STATUS" = "200" ] || [ "$HTTP_STATUS" = "302" ] || [ "$HTTP_STATUS" = "307" ]; then
            echo "✅ Application is healthy (HTTP $HTTP_STATUS)"
          else
            echo "⚠️ Health check returned HTTP $HTTP_STATUS"
            echo "This is not critical - application might still be starting"
          fi

      - name: 📢 Deployment Success
        if: success()
        run: |
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎉 Deployment completed successfully!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🌐 Application URL: https://${{ secrets.PRODUCTION_DOMAIN }}"
          echo "📦 Commit SHA: ${{ github.sha }}"
          echo "👤 Deployed by: ${{ github.actor }}"
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "⏰ Time: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 📢 Deployment Failure
        if: failure()
        run: |
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "❌ Deployment failed!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔍 Check the logs above for details"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
